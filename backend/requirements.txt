# requirements.txt — FastAPI backend dependencies
# Install with: pip install -r requirements.txt

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Async HTTP client (for calling ZAP / Nuclei APIs) ---
httpx==0.27.2
requests==2.32.3

# --- Task queue ---
celery==5.4.0
redis==5.1.1

# --- Database ---
sqlalchemy==2.0.36
asyncpg==0.30.0          # async PostgreSQL driver (used by SQLAlchemy async engine)
psycopg2-binary==2.9.10  # sync driver — kept for Alembic migrations
alembic==1.13.3

# --- Data validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Environment / config ---
python-dotenv==1.0.1

# --- OWASP ZAP Python client ---
python-owasp-zap-v2.4==0.0.21

# --- Utilities ---
tenacity==9.0.0        # retry logic with exponential backoff
python-jose==3.3.0     # JWT (ready for Phase 8 auth)
passlib[bcrypt]==1.7.4 # password hashing (ready for Phase 8 auth)
slowapi==0.1.9         # rate limiting middleware for FastAPI
fpdf2==2.7.9           # PDF report generation
